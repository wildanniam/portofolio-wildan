export default function RuntimeFixturePage() {
  return (
    <main>
      <h1>Server-only runtime fixture</h1>
      <p>This route intentionally contains no client component or interaction.</p>
    </main>
  );
}
